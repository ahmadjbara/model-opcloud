import { ResizeBarDirective } from './resize-bar.directive';

describe('ResizableNewDirective', () => {
  it('should create an instance', () => {
    const directive = new ResizeBarDirective();
    expect(directive).toBeTruthy();
  });
});
